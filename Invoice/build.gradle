group 'invoice'
//version '1.0'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenLocal()
    mavenCentral()
    maven { url "http://repo.spring.io/libs-milestone" }
}

/*jar {
    manifest {
        attributes(
                'Class-Path': configurations.compile.collect { it.getName() }.join(' '), 'Main-Class': 'com.main.java.invoice.project.form.Main'
        )
    }
}*/

jar {
    manifest {
        attributes 'Main-Class' : 'com.main.java.invoice.project.form.Main'
    }
}

dependencies {
    //MySql
    compile("mysql:mysql-connector-java:5.1.40")

    //JSch
    compile("com.jcraft:jsch:0.1.54")

    //Jcalendar
    compile fileTree(dir: 'libs', include: ['*.jar'])
}