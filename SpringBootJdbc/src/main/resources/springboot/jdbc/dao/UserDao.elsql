@NAME(GetTypeByUserId)
    SELECT ID, TYPE FROM MOB_APPLICATION WHERE USER_ID = ?

@NAME(GetLivenessByUserId)
    SELECT MAI.LIVENESS_UUID FROM MOB_APPLICATION MA JOIN MOB_APPLICATION_INFO MAI
        ON MA.ID = MAI.APPLICATION_ID
        WHERE MA.USER_ID = ?